<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pawsome">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/appicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:scaleType="centerCrop"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pawsome">
        <activity
            android:name=".ui.activities.UploadSection"
            android:exported="true" />
        <activity
            android:name=".ui.activities.FavouritesSection"
            android:exported="true" />
        <activity
            android:name=".ui.activities.DogImageDetail"
            android:exported="true" />
        <activity
            android:name=".ui.activities.DogDetails"
            android:exported="true"
            android:label="@string/title_activity_dog_details"
            android:theme="@style/Theme.Pawsome.NoActionBar" />
        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activities.SplashScreen"
            android:exported="true">
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>